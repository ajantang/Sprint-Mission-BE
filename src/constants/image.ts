const MIN_LENGTH_IMAGE = 1;
const MAX_LENGTH_IMAGE = 2048;

export const imageSchema = {
  MIN_LENGTH_IMAGE,
  MAX_LENGTH_IMAGE,
};

export const MIN_NUMBER_IMAGES = 0;
export const MAX_NUMBER_IMAGES = 5;
